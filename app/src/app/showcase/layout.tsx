export const runtime = 'edge';

export const metadata = {
  title: '製作展示影片 | 拾光 Glimmer',
  description: '排列影片、選擇範本、一鍵匯出精美展示影片',
};

export default function ShowcaseLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
